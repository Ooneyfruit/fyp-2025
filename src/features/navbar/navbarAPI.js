/**
 * Navbar Feature Public API.
 * Orchestrates the primary application navigation and contextual actions.
 */

/**
 * Feature Export:
 * Provides the main entry point for the application's top navigation bar.
 */
export { default as NavBar } from './components/NavBar.vue';

// Note: Sub-components like NavUserMenu and NavPracticeSelector remain internal to this feature.